#pragma once

enum SCENE_CREATE
{
	SC_CURRENT,
	SC_NEXT
};
enum MOVE_DIR
{
	MD_LEFT = -1,
	MD_NONE,
	MD_RIGHT
};

enum COLLIDER_TYPE
{
	CT_RECT,
	CT_PIXEL,
	CT_POINT,
	CT_END
};

enum COLLISION_STATE
{
	CS_ENTER,
	CS_STAY,
	CS_LEAVE,
	CS_END
};

enum MONSTER_TYPE
{
	MT_DEFAULT = -1,
	MT_NORMAL,
	MT_KNOCKBACKBOSS,
	MT_BOSS,
	MT_END
};

enum OBJECT_TYPE
{
	OT_DEFAULT = -1,
	OT_PLAYER,
	OT_OTHERPLAYER,
	OT_MONSTER,
	OT_NPC,
	OT_MAP,
	OT_ROPE,
	OT_LADDER,
	OT_PORTAL,
	OT_ITEM,
	OT_SKILL,
	OT_END
};

enum ANIMATION_OPTION
{
	AO_LOOP,
	AO_ONCE_RETURN,
	AO_ONCE_DISABLE,
	AO_ONCE_DESTROY,
	AO_TIME_RETURN,
	AO_TIME_DESTROY,
	AO_END
};

enum SKILL_TYPE
{
	ST_DEFAULT = -1,
	ST_PASSIVE,
	ST_BUFF,
	ST_DEBUFF,
	ST_ATTACK,
	ST_MOVE,
	ST_END
};

enum ITEM_TYPE
{
	IT_DEFAULT = -1,
	IT_EQUIP,
	IT_USABLE,
	IT_ETC,
	IT_INSTALL,
	IT_CASH,
	IT_CLOTH,
	IT_END
};

enum BUTTON_TYPE
{
	BT_NORMAL,
	BT_PRESSED,
	BT_DISABLE,
	BT_MOUSEON,
	BT_END
};

enum KEY_STATE
{
	KS_DEFAULT = -1,
	KS_PRESS,
	KS_DOWN,
	KS_UP,
	KS_END
};

enum TAB_TYPE
{
	TT_ENABLE = 0,
	TT_DISABLE,
	TT_END
};

enum UI_TYPE
{
	UI_DEFAULT = -1,
	UI_MAIN,
	UI_ITEM,
	UI_SKILL,
	UI_STAT,
	UI_KEY,
	UI_MINIMAP,
	UI_SHOP,
	UI_EQUIP,
	UI_END
};

enum ICON_TYPE
{
	IC_DEFAULT = -1,
	IC_ITEM,
	IC_SKILL,
	IC_KEY,
	IC_END
};

enum PACKET_TYPE
{
	PT_DEFAULT = -1,
	PT_CREATEACCOUNT,
	PT_LOGIN,
	PT_STATUS,
	PT_SKILLINFO,
	PT_ITEMINFO,
	PT_EQUIPINFO,
	PT_ADDEQUIP,
	PT_ERASEEQUIP,
	PT_POSITION,
	PT_MOVE,
	PT_OBJECT, // 상시 broadcast용 패킷
	PT_MESSAGE,
	PT_ENTER, // 캐릭터 입장
	PT_LEAVE, // 캐릭터 퇴장
	PT_PORTAL, // 포탈 이용
	PT_SKILL, // 스킬 사용
	PT_CHANGESKILL, // 스킬 변화
	PT_CHANGEITEM, // 아이템 변화
	PT_THROWITEM, // 아이템을 땅에 버림
	PT_GETITEM, // 아이템을 획득함
	PT_DAMAGE, // 데미지 전달
	PT_LEVELUP, // 레벨업 전달
	PT_END
};

enum SOCKET_OPERATION_TYPE
{
	SO_DEFAULT = 1,
	SO_SEND,
	SO_RECV,
	SO_ACCEPT,
	SO_END
};

enum OBJECT_STATE
{
	OBJ_STATE_DEFAULT = -1,
	OBJ_STATE_IDLE,
	OBJ_STATE_RUN,
	OBJ_STATE_JUMP,
	OBJ_STATE_ATTACK,
	OBJ_STATE_HIT,
	OBJ_STATE_PRONE,
	OBJ_STATE_ROPE,
	OBJ_STATE_LADDER,
	OBJ_STATE_END
};

enum WINDOW_STATE
{
	WS_WRONGID,
	WS_EXISTID,
	WS_WRONGPW,
	WS_CONNECTING,
	WS_END
};

enum EQUIP_TYPE
{
	ET_DEFAULT = -1,
	ET_HEAD,
	ET_CLOTH,
	ET_END
};